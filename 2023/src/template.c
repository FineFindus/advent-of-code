#include "../shared/aoc.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

unsigned
part_one (char *input)
{
  return 0;
}

unsigned
part_two (char *input)
{
  return 0;
}

int
main (int argc, char *argv[])
{
  print_result (part_one, 0);
  print_result (part_two, 0);
  return EXIT_SUCCESS;
}
